{% extends "base.html" %}

{% block page_content %}
<div class="page-header">
<h1>Post list</h1>
</div>
<div>
  {% for post in post_list %}
    <h4>
      <a href="{{url_for('show_post', name = post.path.replace('posts/',''))}}">{{post.title}}</a>
      <br>
      <small>{{post.published}}</samll>
      <br>
    </h4>
  {% endfor %}
</div>
{% endblock %}
